name:  test
on:
  push:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: add
      run: |
        gh api \
          -H "Accept: application/vnd.github.v3+json" \
          /repos/OWNER/REPO/pulls
