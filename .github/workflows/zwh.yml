name: 'Merge branch'
on:
  push

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Git merge operation
        uses: alexrogalskiy/github-action-git-operation@V2.0.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          mode: 'merge-fast-forward'
          sourceBranch: 'main'
          targetBranch: 'merge-test'         



     
    
